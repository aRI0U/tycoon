package tycoon.game

import tycoon.ui.Tile

class MapGenerator {
  private val r = scala.util.Random
/*
  def generateRandomMap(game: Game): TileMap = {
    val tilemap = new TileMap(width = game.map_width, height = game.map_height)
    tilemap.fillBackground(Tile.grass)

    tilemap
  }*/
}
